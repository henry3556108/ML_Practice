import pandas as pd
import numpy as np
# import eli5
# from sklearn.ensemble import RandomForestClassifier as tree

# clf = tree
# eli5.show_weights(clf)
df = pd.DataFrame(columns=['a','b'])

print(df)
# ls = np.array([0.95,0.9])
# print(np.argmax(ls))
# print(ls)
# # a = np.zeros([10,10])
# # b = np.zeros([10,10])
# a = [1,1,1]
# b = [1,0,1]
# # a[0][0] = 1
# # a[0][1] = 1
# # b[0][0] = 1
# print(np.mean(a==b))